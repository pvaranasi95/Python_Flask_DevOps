apiVersion: v1
kind: Service
metadata: 
    name: flaskapp
spec:
  type: NodePort
  selector:
    app: flaskapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 5000
   